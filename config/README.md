# Config

This directory will hold local YAML configuration for deterministic alert triage.

The files in this directory began as data-only scaffold placeholders. They must not
contain executable code, network endpoints, API keys, real credentials, real
customer data, or external threat intelligence references.

The project is offline-only and limited to synthetic alert data.
